import math

radius=float(input("Enter radius of circle: "))       # Accept radius from user
area=math.pi * radius * radius                        # Calculate
print(f'Area of circle of radius {radius} = {area}')  # Display output